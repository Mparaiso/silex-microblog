{% extends 'common/layout.html.twig' %}
{% block content %}
    <div class="row">
        {% for post in posts %}
            <div class="span12">
                {{ post.author.nickname }} wrote : <b>{{ post.body }}</b></div>
        {% endfor %}

    </div>
{% endblock %}